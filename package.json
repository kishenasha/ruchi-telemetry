{
  "name": "ruchi-telemetry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply ruchi-telemetry --remote",
    "migrate:local": "wrangler d1 migrations apply ruchi-telemetry --local",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
